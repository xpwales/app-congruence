CREATE TABLE `APP_GRP_relation` (
  `APP_id` INT UNSIGNED NOT NULL,
  `GRP_id` INT UNSIGNED NOT NULL,
  PRIMARY KEY (`APP_id`, `GRP_id`),
  INDEX `fk_GRP_id_idx` (`GRP_id` ASC),
  CONSTRAINT `fk_APP_GRP_APP_id`
  FOREIGN KEY (`APP_id`)
  REFERENCES `APP_application` (`APP_id`)
    ON DELETE RESTRICT
    ON UPDATE RESTRICT,
  CONSTRAINT `fk_APP_GRP_GRP_id`
  FOREIGN KEY (`GRP_id`)
  REFERENCES `GRP_group` (`GRP_id`)
    ON DELETE RESTRICT
    ON UPDATE RESTRICT)
  DEFAULT CHARACTER SET 'utf8' COLLATE utf8_general_ci
  ENGINE = INNODB;