CREATE TABLE `APP_DIR_relation` (
  `APP_id` INT UNSIGNED NOT NULL,
  `DIR_id` INT UNSIGNED NOT NULL,
  PRIMARY KEY (`APP_id`, `DIR_id`),
  INDEX `fk_DIR_id_idx` (`DIR_id` ASC),
  CONSTRAINT `fk_APP_DIR_APP_id`
  FOREIGN KEY (`APP_id`)
  REFERENCES `APP_application` (`APP_id`)
    ON DELETE RESTRICT
    ON UPDATE RESTRICT,
  CONSTRAINT `fk_APP_DIR_DIR_id`
  FOREIGN KEY (`DIR_id`)
  REFERENCES `DIR_directory` (`DIR_id`)
    ON DELETE RESTRICT
    ON UPDATE RESTRICT)
  DEFAULT CHARACTER SET 'utf8' COLLATE utf8_general_ci
  ENGINE = INNODB;

CREATE TABLE `APP_BRND_relation` (
  `APP_id` INT UNSIGNED NOT NULL,
  `BRND_id` INT UNSIGNED NOT NULL,
  PRIMARY KEY (`APP_id`, `BRND_id`),
  INDEX `fk_BRND_id_idx` (`BRND_id` ASC),
  CONSTRAINT `fk_APP_BRND_APP_id`
  FOREIGN KEY (`APP_id`)
  REFERENCES `APP_application` (`APP_id`)
    ON DELETE RESTRICT
    ON UPDATE RESTRICT,
  CONSTRAINT `fk_APP_BRND_BRND_id`
  FOREIGN KEY (`BRND_id`)
  REFERENCES `BRND_brand` (`BRND_id`)
    ON DELETE RESTRICT
    ON UPDATE RESTRICT)
  DEFAULT CHARACTER SET 'utf8' COLLATE utf8_general_ci
  ENGINE = INNODB;